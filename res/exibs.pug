doctype html
html(lang="hu")
	head
		title 3/7Galery
		link(href='main.css', rel='stylesheet')
		meta(charset='utf-8')
	body
		include nav.pug
		main
			.eventcont
				|<?php
				|	$servername = "localhost";
				|	$username = "haromhet_hu";
				|	$password = "fdjqyoxl";
				|	$dbname = "haromhet_hu_legnewebb";
				|	$conn = new mysqli($servername, $username, $password, $dbname);
				|	if ($conn->connect_error) {
				|		die("Connection failed: " . $conn->connect_error);
				|		echo "bajvan";
				|	}
				|	mysqli_set_charset($conn,"utf8");
				|	$sql = "SELECT * FROM `kiallitasok` ORDER BY date DESC LIMIT 9";
				|	$result = $conn->query($sql);
				|	if ($result->num_rows > 0) {
				|        while($row = $result->fetch_assoc()) {
				|          echo "
				|          <div class='exibcard'>
				|            <img src='img/uploads/".$row["kep"]."'>
				|            <h4>" . $row["title"] . "</h4>
				|            <p>".$row["date"]."</p>
				|            <p class='leiras'>" . $row["szoveg"] . "</p>
				|            <button>Bővebben</button>
				|          </div> ";
				|        }
				|      }
				|	$conn->close();
				|?>
	include footer.pug